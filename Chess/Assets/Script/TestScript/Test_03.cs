using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test_03 : MonoBehaviour
{

    // int   i_number = 100;
    // float f_number = 1.0f;
    // string s_text = "キャストテスト";

    // Start is called before the first frame update
    void Start()
    {
        // // 整数型のキャスト
        // i_number = (int)f_number

        // // 小数点型のキャスト
        // f_number = (float)i_number;

        // // 文字列のキャスト
        // s_text = "" + i_number;
        // Debug.Log(s_text); //100 -> string型の「100」です

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
